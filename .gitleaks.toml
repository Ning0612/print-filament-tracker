title = "filament-ledger gitleaks configuration"

[extend]
useDefault = true

[[allowlists]]
description = "Historical documentation placeholder; not a Bambu Cloud credential"
regexTarget = "match"
regexes = ["your-token>\\s*BAMBU_REGION=global"]
